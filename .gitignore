node_modules/
# Node identities, cached builds, ledgers, deltas — private keys live here.
data/
.env
*.log
# Packed zips are build output: npm run pack
dist/
*.zip
# Vercel project linkage (local machine ids)
cabinet/.vercel/
.DS_Store
Thumbs.db

# audit scratch (the auditor's temp settlement stores and raw runner output; the review itself is tracked)
audit/probe-data-*/
audit-test-output.txt
.agents/

# An operator install writes its node.env beside the code; never commit one
/node.env

# Local wallet signer (tools/signer): internal tool, kept out of the repo on purpose (25 Sep 2026)
tools/signer/
